name: CreateUserInfo
description: 测试创建用户信息用例
steps:
  - api: UserLogin
    action: get_token
    actualParameter: ["xiaohong","123123"]
    saveGlobal:
      token: data.token
    asserts:
      - actual: code
        matcher: equalTo
        expect: '00000'
        reason: 'token获取成功'
  - api: UserInfo
    action: add_user_info
    actualParameter: ["${getRandomInt}","xiaohong${getRandomInt}"]
    asserts:
      - actual: code
        matcher: equalTo
        expect: '00000'
        reason: '创建用户信息成功'
      - actual: message
        matcher: equalTo
        expect: '成功'
        reason: '创建用户信息成功'
